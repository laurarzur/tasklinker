{% extends 'base.html.twig' %}

{% block title %}
	{{ employe.prenom }}
	{{ employe.nom }}
{% endblock %}

{% block heading %}
	{{ employe.prenom }}
	{{ employe.nom }}
{% endblock %}

{% block body %}
{{ form_start(form) }}
<div>
	{{ form_row(form.nom, {label: 'Nom'}) }}
</div>
<div>
	{{ form_row(form.prenom, {label: 'Prenom'}) }}
</div>
<div>
	{{ form_row(form.email, {label: 'Email'}) }}
</div>
<div class="form-flex">
	<div>
		{{ form_row(form.dateEntree, {label: 'Date d\'entrée'}) }}
    </div>
    <div>
        {{ form_row(form.statut, {label: 'Statut'}) }}
    </div>
</div>
	<br>
	<button class="button button-submit">Enregistrer</button>
	{{ form_end(form) }}
{% endblock %}
